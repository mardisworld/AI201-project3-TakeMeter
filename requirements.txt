# --- web interface (app.py) ---
gradio>=6.9.0

# --- fine-tuning + inference (train.py, app.py) ---
# pinned to the 4.x line: transformers 5.x conflicts with sentence-transformers 3.4.1
transformers>=4.41.0,<5.0.0
torch
scikit-learn
accelerate>=0.26.0

# --- zero-shot baseline (baseline.py) ---
groq==0.15.0
python-dotenv==1.0.1
